# markdownview-- 预览示例

编辑这个文件时，右侧预览会自动刷新。

[跳到长文档测试](#长文档与滚动测试)

## 常用语法

- [x] 实时预览
- [x] 表格
- [ ] 后续扩展 Mermaid 和数学公式

| 功能 | 状态 |
| --- | --- |
| GitHub Markdown 方言 | 已支持 |
| 本地相对图片 | 已支持 |
| HTML 导出 | 已支持 |

> 滚动左侧编辑器，预览窗口会按行数比例跟随。

```cpp
document->setMarkdown(markdown, QTextDocument::MarkdownDialectGitHub);
```

[访问 notepad-- 项目](https://github.com/cxasm/notepad--)

行内代码示例：`Ctrl+Shift+M`。拖选上面的链接文字时不应打开浏览器；短点击链接时才打开。

## 相对路径图片

下面的图片路径包含中文和空格。预览与导出的单文件 HTML 都应显示它；HTML 中应转换为
data URL，且原预览内容不应被导出操作改写。

![相对路径测试图片](assets/相对 图片.xpm)

## 主题测试

切换 notepad-- 明暗主题，检查正文、[链接](https://example.com/theme)、引用、行内代码、
代码块和表格均保持可读；切换主题前后阅读位置不应跳回顶部。

> 这段引用用于检查明暗主题下的边框、背景和文字对比度。

## 长文档与滚动测试

以下编号段落用于制造足够的滚动范围。关闭同步滚动后停在中部编辑并等待刷新，预览应恢复
原阅读位置；重新开启同步滚动后，应由编辑器位置重新主导。连续快速输入只应在停顿后刷新。

1. 生命周期：创建预览后关闭标签，再关闭 Dock 和宿主窗口。
2. 多窗口：同时打开两个宿主窗口，各自显示、隐藏和刷新预览。
3. 快速切换：在两个 Markdown 标签间切换并立即输入，预览只显示当前文档。
4. 路径变化：将未命名文档保存到含中文和空格的目录，再改名为 `.txt` 和 `.md`。
5. 导出快照：隐藏 Dock 后编辑并导出，取消保存不产生文件，再确认相对图片已嵌入。
6. 链接锚点：点击页首锚点、HTTPS 和相对链接，拖选链接文字不应误打开。
7. 主题刷新：在长文档中部切换明暗主题，格式和滚动位置保持稳定。
8. 大文档：复制本节直到文件超过 1 MiB，确认后续编辑进入手工刷新模式并能取得最终内容。
